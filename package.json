{
  "name": "@aptos-labs/sign-in-with-aptos",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "turbo format",
    "check": "turbo test format lint",
    "clean": "turbo clean && rm -rf node_modules",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo build --filter=@aptos-labs/siwa && changeset publish",
    "vite-example": "turbo dev --filter=@aptos-labs/siwa-vite-example --filter=@aptos-labs/siwa-backend-example --ui tui"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "^2.27.11",
    "@types/node": "22.13.4",
    "@vitest/coverage-v8": "^3.0.5",
    "turbo": "^2.4.2",
    "vitest": "^3.0.5"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22.14.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "@swc/core", "esbuild"]
  }
}
